# Signal Processing Gallery

Self-contained Python demonstrations covering time-frequency analysis,
decomposition methods, and their applications to biomedical signals.

Run any script locally with:

```bash
uv run python src/<topic>/<script>.py
```

Each script generates PNG figures saved alongside the source file.
